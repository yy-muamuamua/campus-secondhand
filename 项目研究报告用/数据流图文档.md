# 校园二手交易平台 - 数据流图与UML文档

## 目录
1. [顶层数据流图](#1-顶层数据流图)
2. [用户认证与授权](#2-用户认证与授权)
3. [商品管理流程](#3-商品管理流程)
4. [留言与收藏](#4-留言与收藏)
5. [管理员后台](#5-管理员后台)
6. [完整数据流示例](#6-完整数据流示例)
7. [数据结构关系图](#7-数据结构关系图)
8. [状态管理数据流](#8-状态管理数据流)
9. [UML用例图](#9-uml用例图)
10. [数据库设计](#10-数据库设计)

---

## 1. 顶层数据流图

### 主要实体与流程
```mermaid
flowchart TD
    User[普通用户] -->|浏览商品| System[校园二手交易平台]
    User -->|发布商品| System
    User -->|收藏商品| System
    User -->|留言互动| System
    
    Admin[管理员] -->|管理商品| System
    Admin -->|管理用户| System
    Admin -->|管理留言| System
    
    System -->|返回商品列表| User
    System -->|保存商品信息| System
    System -->|发送通知| User
```

### 核心数据存储
| 数据存储 | 说明 |
|---------|------|
| 用户表 | 存储用户信息 |
| 商品表 | 存储商品信息 |
| 收藏表 | 存储收藏关系 |
| 留言表 | 存储留言记录 |

---

## 2. 用户认证与授权

### 2.1 用户注册流程
```mermaid
flowchart LR
    User[用户] -->|输入邮箱| SendCode[发送验证码]
    SendCode -->|验证码发送成功| User
    User -->|填写注册信息| VerifyCode[验证验证码]
    VerifyCode -->|验证成功| Register[注册用户]
    Register -->|创建账户| Login[自动登录]
    Login -->|返回Token| User
    Login -->|保存用户信息| UserStore[用户状态存储]
```

### 2.2 用户登录流程
```mermaid
flowchart LR
    User[用户] -->|输入邮箱密码| LoginAPI[登录API]
    LoginAPI -->|验证成功| ReturnToken[返回Token]
    ReturnToken -->|保存Token| Storage[本地存储]
    Storage -->|获取用户信息| UserAPI[获取用户信息API]
    UserAPI -->|更新状态| UserStore[用户状态管理]
    UserStore -->|跳转首页| Router[路由跳转]
    
    LoginAPI -->|验证失败| ShowError[显示错误提示]
```

---

## 3. 商品管理流程

### 3.1 商品发布流程
```mermaid
flowchart LR
    User[用户] -->|填写商品信息| Validate[表单验证]
    Validate -->|验证通过| UploadImage[上传图片]
    UploadImage -->|上传成功| PublishAPI[发布商品API]
    PublishAPI -->|保存商品| GoodsDB[商品数据库]
    GoodsDB -->|返回成功| ShowSuccess[显示成功提示]
    ShowSuccess -->|跳转首页| Router[路由跳转]
    
    UploadImage -->|上传失败| ShowError[显示失败提示]
    Validate -->|验证失败| ShowError
```

### 3.2 商品浏览与搜索
```mermaid
flowchart LR
    User[用户] -->|进入首页| LoadList[加载商品列表]
    LoadList -->|调用API| GoodsAPI[商品列表API]
    GoodsAPI -->|返回数据| RenderList[渲染商品列表]
    
    User -->|选择分类| FilterByCategory[按分类筛选]
    User -->|输入关键词| Search[搜索商品]
    User -->|排序| Sort[排序结果]
    
    FilterByCategory -->|刷新列表| LoadList
    Search -->|刷新列表| LoadList
    Sort -->|刷新列表| LoadList
    
    RenderList -->|下拉加载| LoadMore[加载更多]
    LoadMore -->|分页获取| GoodsAPI
```

---

## 4. 留言与收藏

### 4.1 留言流程
```mermaid
flowchart LR
    User[用户] -->|输入留言| SubmitMessage[提交留言]
    SubmitMessage -->|验证登录| CheckAuth[检查登录状态]
    CheckAuth -->|已登录| AddMessageAPI[添加留言API]
    CheckAuth -->|未登录| RedirectLogin[跳转登录页]
    
    AddMessageAPI -->|保存留言| MessagesDB[留言数据库]
    MessagesDB -->|返回成功| RefreshList[刷新留言列表]
    RefreshList -->|显示新留言| DetailPage[详情页]
```

### 4.2 收藏流程
```mermaid
flowchart LR
    User[用户] -->|点击收藏| ToggleFavorite[切换收藏状态]
    ToggleFavorite -->|检查状态| CheckFavorite[检查是否已收藏]
    
    CheckFavorite -->|未收藏| AddFavorite[添加收藏]
    CheckFavorite -->|已收藏| RemoveFavorite[取消收藏]
    
    AddFavorite -->|调用API| AddAPI[添加收藏API]
    RemoveFavorite -->|调用API| RemoveAPI[取消收藏API]
    
    AddAPI -->|更新状态| UpdateUI[更新UI按钮]
    RemoveAPI -->|更新状态| UpdateUI
```

---

## 5. 管理员后台

### 5.1 商品管理
```mermaid
flowchart LR
    Admin[管理员] -->|进入后台| AdminPage[管理后台]
    AdminPage -->|加载商品| GoodsListAPI[商品列表API]
    AdminPage -->|修改状态| UpdateStatus[更新商品状态]
    AdminPage -->|删除商品| DeleteGoods[删除商品]
    
    UpdateStatus -->|调用API| UpdateStatusAPI[更新状态API]
    DeleteGoods -->|调用API| DeleteAPI[删除API]
    
    UpdateStatusAPI -->|刷新列表| RefreshGoods[刷新商品列表]
    DeleteAPI -->|刷新列表| RefreshGoods
```

### 5.2 用户管理
```mermaid
flowchart LR
    Admin[管理员] -->|查看用户| UserListAPI[用户列表API]
    UserListAPI -->|显示用户| RenderUsers[渲染用户列表]
    
    Admin -->|封禁用户| BanUser[封禁用户]
    Admin -->|解封用户| UnbanUser[解封用户]
    
    BanUser -->|调用API| UpdateUserAPI[更新用户状态API]
    UnbanUser -->|调用API| UpdateUserAPI
    
    UpdateUserAPI -->|更新状态| UserDB[用户数据库]
    UserDB -->|刷新列表| RefreshUsers[刷新用户列表]
```

---

## 6. 完整数据流示例

### 用户发布商品的完整数据流
```mermaid
sequenceDiagram
    participant User as 用户
    participant Frontend as 前端
    participant API as API层
    participant Server as 后端服务
    participant DB as 数据库
    participant Storage as 云存储
    
    User->>Frontend: 填写商品信息
    Frontend->>Frontend: 表单验证
    User->>Frontend: 上传图片
    Frontend->>Storage: 上传图片文件
    Storage-->>Frontend: 返回图片URL
    Frontend->>API: publishGoods()
    API->>Server: POST /goods
    Server->>DB: 插入商品记录
    DB-->>Server: 返回商品ID
    Server-->>API: 返回成功响应
    API-->>Frontend: 返回Promise
    Frontend->>Frontend: 显示成功提示
    Frontend->>Frontend: 跳转到首页
```

---

## 7. 数据结构关系图

```mermaid
erDiagram
    USER ||--o{ GOODS : "发布"
    USER ||--o{ FAVORITE : "收藏"
    USER ||--o{ MESSAGE : "发布"
    GOODS ||--o{ FAVORITE : "被收藏"
    GOODS ||--o{ MESSAGE : "有留言"
    
    USER {
        string _id "PK"
        string email
        string nickname
        string avatar
        string role
        boolean emailVerified
        boolean disabled
        string createTime
    }
    
    GOODS {
        string _id "PK"
        string title
        number price
        string category
        string images
        string sellerId "FK"
        string status
        number views
        string createTime
    }
    
    FAVORITE {
        string _id "PK"
        string userId "FK"
        string goodsId "FK"
        string createTime
    }
    
    MESSAGE {
        string _id "PK"
        string userId "FK"
        string goodsId "FK"
        string content
        string reply
        string createTime
    }
```

---

## 8. 状态管理数据流

### Pinia Store 数据流
```mermaid
flowchart LR
    API[API调用] -->|更新数据| Store[Pinia Store]
    Store -->|响应式更新| Component[Vue组件]
    Component -->|用户操作| Action[Action方法]
    Action -->|调用API| API
    Store -->|持久化| Storage[本地存储]
    Storage -->|初始化| Store
```

---

## 9. UML用例图

### 9.1 系统整体用例图
```mermaid
sequenceDiagram
    actor User as 用户
    actor Admin as 管理员
    
    User->>校园二手平台: 浏览商品列表
    User->>校园二手平台: 搜索商品
    User->>校园二手平台: 查看商品详情
    User->>校园二手平台: 发布商品
    User->>校园二手平台: 编辑商品
    User->>校园二手平台: 删除商品
    User->>校园二手平台: 收藏商品
    User->>校园二手平台: 取消收藏
    User->>校园二手平台: 发布留言
    User->>校园二手平台: 回复留言
    User->>校园二手平台: 登录
    User->>校园二手平台: 注册
    User->>校园二手平台: 退出登录
    User->>校园二手平台: 查看个人信息
    User->>校园二手平台: 修改个人信息
    
    Admin->>校园二手平台: 查看统计数据
    Admin->>校园二手平台: 管理商品
    Admin->>校园二手平台: 管理用户
    Admin->>校园二手平台: 管理留言
```

### 9.2 用户用例详细图
```mermaid
flowchart LR
    subgraph 用户功能
        A[浏览商品] --> B[搜索商品]
        B --> C[查看详情]
        C --> D[收藏商品]
        C --> E[发布留言]
        
        F[登录] --> G[发布商品]
        G --> H[编辑商品]
        H --> I[删除商品]
        
        F --> J[查看收藏]
        F --> K[查看留言]
        F --> L[个人中心]
    end
    
    subgraph 前置条件
        F -.-> 需要登录
        G -.-> 需要登录
        H -.-> 需要登录
        I -.-> 需要登录
        D -.-> 需要登录
        E -.-> 需要登录
        J -.-> 需要登录
        K -.-> 需要登录
        L -.-> 需要登录
    end
```

### 9.3 管理员用例图
```mermaid
flowchart LR
    subgraph 管理员功能
        A[查看统计数据]
        
        B[商品管理] --> C[查看商品列表]
        B --> D[修改商品状态]
        B --> E[删除商品]
        
        F[用户管理] --> G[查看用户列表]
        F --> H[封禁用户]
        F --> I[解封用户]
        
        J[留言管理] --> K[查看留言列表]
        J --> L[删除留言]
    end
    
    subgraph 前置条件
        A -.-> 需要管理员权限
        B -.-> 需要管理员权限
        F -.-> 需要管理员权限
        J -.-> 需要管理员权限
    end
```

### 9.4 用例描述表

| 用例名称 | 参与者 | 前置条件 | 基本流程 | 后置条件 |
|---------|--------|---------|---------|---------|
| 浏览商品 | 用户 | 无 | 进入首页 → 显示商品列表 | 商品列表展示成功 |
| 搜索商品 | 用户 | 无 | 输入关键词 → 点击搜索 | 显示搜索结果 |
| 发布商品 | 用户 | 已登录 | 填写信息 → 上传图片 → 提交 | 商品发布成功 |
| 收藏商品 | 用户 | 已登录 | 点击收藏按钮 | 商品加入收藏 |
| 发布留言 | 用户 | 已登录 | 输入内容 → 提交 | 留言发布成功 |
| 登录 | 用户 | 未登录 | 输入邮箱密码 → 提交 | 登录成功 |
| 注册 | 用户 | 未登录 | 输入信息 → 验证验证码 → 提交 | 注册成功 |
| 查看统计 | 管理员 | 已登录且为管理员 | 进入后台 → 查看统计 | 显示统计数据 |
| 管理商品 | 管理员 | 已登录且为管理员 | 查看列表 → 操作商品 | 商品状态更新 |
| 管理用户 | 管理员 | 已登录且为管理员 | 查看列表 → 封禁/解封 | 用户状态更新 |
| 管理留言 | 管理员 | 已登录且为管理员 | 查看列表 → 删除留言 | 留言删除成功 |

---

## 10. 数据库设计

### 10.1 数据库表设计

#### 10.1.1 用户表 (users)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| _id | ObjectId | PRIMARY KEY | 用户唯一标识 |
| email | String | NOT NULL, UNIQUE | 邮箱地址 |
| password | String | NOT NULL | 加密后的密码 |
| nickname | String | NOT NULL | 用户昵称 |
| avatar | String | | 头像URL |
| studentId | String | UNIQUE | 学号 |
| role | String | NOT NULL, DEFAULT 'student' | 用户角色: guest/student/admin |
| emailVerified | Boolean | DEFAULT false | 邮箱是否验证 |
| contactWechat | String | | 微信号 |
| contactPhone | String | | 手机号 |
| disabled | Boolean | DEFAULT false | 是否被封禁 |
| createTime | DateTime | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| lastLogin | DateTime | | 最后登录时间 |

**索引设计**：
- `email` - 唯一索引（登录查询）
- `studentId` - 唯一索引（学生认证）
- `role` - 普通索引（权限过滤）
- `disabled` - 普通索引（封禁查询）

---

#### 10.1.2 商品表 (goods)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| _id | ObjectId | PRIMARY KEY | 商品唯一标识 |
| title | String | NOT NULL | 商品标题 |
| description | String | NOT NULL | 商品描述 |
| price | Number | NOT NULL | 价格 |
| category | String | NOT NULL | 分类: 书籍/电子产品/生活用品/其他 |
| images | Array<String> | NOT NULL | 图片URL数组 |
| sellerId | ObjectId | NOT NULL, FOREIGN KEY | 卖家ID |
| status | String | NOT NULL, DEFAULT 'on' | 状态: on/off/sold |
| views | Number | DEFAULT 0 | 浏览次数 |
| createTime | DateTime | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updateTime | DateTime | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- `sellerId` - 普通索引（卖家商品查询）
- `status` - 普通索引（状态过滤）
- `category` - 普通索引（分类过滤）
- `createTime` - 普通索引（时间排序）

---

#### 10.1.3 收藏表 (favorites)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| _id | ObjectId | PRIMARY KEY | 收藏唯一标识 |
| userId | ObjectId | NOT NULL, FOREIGN KEY | 用户ID |
| goodsId | ObjectId | NOT NULL, FOREIGN KEY | 商品ID |
| createTime | DateTime | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- `userId` - 普通索引（用户收藏查询）
- `goodsId` - 普通索引（商品收藏查询）
- `(userId, goodsId)` - 唯一复合索引（防止重复收藏）

---

#### 10.1.4 留言表 (messages)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| _id | ObjectId | PRIMARY KEY | 留言唯一标识 |
| goodsId | ObjectId | NOT NULL, FOREIGN KEY | 商品ID |
| userId | ObjectId | NOT NULL, FOREIGN KEY | 用户ID |
| content | String | NOT NULL | 留言内容 |
| reply | String | | 回复内容 |
| createTime | DateTime | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| replyTime | DateTime | | 回复时间 |

**索引设计**：
- `goodsId` - 普通索引（商品留言查询）
- `userId` - 普通索引（用户留言查询）
- `createTime` - 普通索引（时间排序）

---

#### 10.1.5 验证码表 (verification_codes)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| _id | ObjectId | PRIMARY KEY | 记录唯一标识 |
| email | String | NOT NULL | 邮箱地址 |
| code | String | NOT NULL | 验证码 |
| expiresAt | DateTime | NOT NULL | 过期时间 |
| used | Boolean | DEFAULT false | 是否已使用 |
| createTime | DateTime | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- `email` - 普通索引（验证码查询）
- `expiresAt` - 普通索引（过期清理）

---

### 10.2 数据库关系图

```mermaid
erDiagram
    USERS ||--o{ GOODS : "发布"
    USERS ||--o{ FAVORITES : "收藏"
    USERS ||--o{ MESSAGES : "留言"
    GOODS ||--o{ FAVORITES : "被收藏"
    GOODS ||--o{ MESSAGES : "有留言"
    
    USERS {
        ObjectId _id "PK"
        String email "UK"
        String password
        String nickname
        String avatar
        String studentId "UK"
        String role
        Boolean emailVerified
        Boolean disabled
        DateTime createTime
        DateTime lastLogin
    }
    
    GOODS {
        ObjectId _id "PK"
        String title
        String description
        Number price
        String category
        String images
        ObjectId sellerId "FK"
        String status
        Number views
        DateTime createTime
        DateTime updateTime
    }
    
    FAVORITES {
        ObjectId _id "PK"
        ObjectId userId "FK"
        ObjectId goodsId "FK"
        DateTime createTime
    }
    
    MESSAGES {
        ObjectId _id "PK"
        ObjectId goodsId "FK"
        ObjectId userId "FK"
        String content
        String reply
        DateTime createTime
        DateTime replyTime
    }
    
    VERIFICATION_CODES {
        ObjectId _id "PK"
        String email
        String code
        DateTime expiresAt
        Boolean used
    }
```

### 10.3 ER关系说明

| 关系 | 说明 | 基数 |
|------|------|------|
| 用户 - 商品 | 用户发布商品 | 1:N |
| 用户 - 收藏 | 用户收藏商品 | 1:N |
| 用户 - 留言 | 用户发布留言 | 1:N |
| 商品 - 收藏 | 商品被收藏 | 1:N |
| 商品 - 留言 | 商品有留言 | 1:N |

### 10.4 数据库操作示例

#### 10.4.1 查询商品列表（分页+筛选）
```javascript
db.goods.find({ 
  status: 'on',
  category: '电子产品',
  price: { $gte: 100, $lte: 1000 }
})
.sort({ createTime: -1 })
.skip((page - 1) * pageSize)
.limit(pageSize)
.populate('sellerId', 'nickname avatar')
```

#### 10.4.2 查询用户收藏
```javascript
db.favorites.find({ userId: ObjectId('xxx') })
.sort({ createTime: -1 })
.populate('goodsId', 'title price images category')
```

#### 10.4.3 查询商品留言
```javascript
db.messages.find({ goodsId: ObjectId('xxx') })
.sort({ createTime: -1 })
.populate('userId', 'nickname avatar')
```

### 10.5 数据字典

#### 10.5.1 用户角色枚举
| 值 | 说明 |
|----|------|
| guest | 游客（未注册） |
| student | 学生用户（已注册/认证） |
| admin | 管理员 |

#### 10.5.2 商品状态枚举
| 值 | 说明 |
|----|------|
| on | 上架中 |
| off | 已下架 |
| sold | 已售出 |

#### 10.5.3 商品分类枚举
| 值 | 说明 |
|----|------|
| 书籍 | 图书教材 |
| 电子产品 | 数码设备 |
| 生活用品 | 日常用品 |
| 其他 | 其他类别 |

### 10.6 数据安全与合规

#### 10.6.1 加密存储
- **密码**：使用 bcrypt 或 argon2 加密存储
- **敏感字段**：手机号、微信号等敏感信息可考虑脱敏存储

#### 10.6.2 访问控制
| 角色 | 用户表 | 商品表 | 收藏表 | 留言表 |
|------|--------|--------|--------|--------|
| 游客 | 只读(公开) | 只读(公开) | 无 | 只读(公开) |
| 学生 | 读写(本人) | 读写(本人) | 读写(本人) | 读写(本人) |
| 管理员 | 读写(全部) | 读写(全部) | 只读 | 读写(全部) |

#### 10.6.3 数据清理策略
- **验证码**：超过过期时间自动删除
- **软删除**：商品/留言支持软删除而非物理删除
- **日志保留**：操作日志保留90天

### 10.7 性能优化建议

1. **索引优化**：根据查询频率添加复合索引
2. **数据分片**：当数据量较大时考虑分片存储
3. **缓存策略**：热门商品数据缓存到 Redis
4. **读写分离**：主从复制，读操作走从库

---

**文档版本**: v1.0  
**生成日期**: 2026-05-16  
**适用项目**: 校园二手交易平台
