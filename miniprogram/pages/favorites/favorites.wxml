<view class="page">
  <view class="goods-list">
    <view wx:for="{{goodsList}}" wx:key="_id" class="goods-item" data-id="{{item._id}}" bindtap="goToDetail">
      <image src="{{item.images && item.images[0] ? item.images[0] : '/images/default.png'}}" mode="aspectFill" />
      <view class="info">
        <text class="title">{{item.title}}</text>
        <text class="price">¥{{item.price}}</text>
      </view>
    </view>
  </view>

  <view class="empty" wx:if="{{!loading && goodsList.length === 0}}">
    暂无收藏商品
  </view>
  <view class="loading" wx:if="{{loading}}">加载中...</view>
</view>